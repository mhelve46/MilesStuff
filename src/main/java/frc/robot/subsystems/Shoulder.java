// RobotBuilder Version: 6.1
//
// This file was generated by RobotBuilder. It contains sections of
// code that are automatically generated and assigned by robotbuilder.
// These sections will be updated in the future when you export to
// Java from RobotBuilder. Do not put any code or make any change in
// the blocks indicating autogenerated code or it will be lost on an
// update. Deleting the comments indicating the section will prevent
// it from being updated in the future.

// ROBOTBUILDER TYPE: Subsystem.

package frc.robot.subsystems;


import com.ctre.phoenix6.StatusCode;
import com.ctre.phoenix6.configs.CurrentLimitsConfigs;
import com.ctre.phoenix6.configs.MotionMagicConfigs;
import com.ctre.phoenix6.configs.TalonFXConfiguration;
import com.ctre.phoenix6.controls.MotionMagicVoltage;
import com.ctre.phoenix6.controls.NeutralOut;
import com.ctre.phoenix6.controls.PositionTorqueCurrentFOC;
import com.ctre.phoenix6.controls.PositionVoltage;
import com.ctre.phoenix6.hardware.TalonFX;

import edu.wpi.first.wpilibj.DigitalInput;
import edu.wpi.first.wpilibj.smartdashboard.SmartDashboard;
import edu.wpi.first.wpilibj2.command.SubsystemBase;
import frc.robot.Constants;

import static edu.wpi.first.units.Units.Amps;
import static edu.wpi.first.units.Units.RotationsPerSecond;
import static edu.wpi.first.units.Units.RotationsPerSecondPerSecond;
import static edu.wpi.first.units.Units.Second;
import static edu.wpi.first.units.Units.Volts;

// BEGIN AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=IMPORTS

    // END AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=IMPORTS


/**
 *
 */
public class Shoulder extends SubsystemBase {
    // BEGIN AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=CONSTANTS

    // END AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=CONSTANTS
    /* Start at position 0, use slot 0 */
    private final PositionVoltage m_positionVoltage = new PositionVoltage(0).withSlot(0);
    /* Start at position 0, use slot 1 */
    private final NeutralOut m_brake = new NeutralOut();
    private final PositionTorqueCurrentFOC m_positionTorque = new PositionTorqueCurrentFOC(0).withSlot(1);
    // BEGIN AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=DECLARATIONS
    private TalonFX shoulderMotor;
    // END AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=DECLARATIONS
        private DigitalInput shoulderFrontSwitch;
    private DigitalInput shoulderBackSwitch;
    private final MotionMagicVoltage m_motionMagicReq = new MotionMagicVoltage(0).withSlot(0);
    private double shoulderGoalPos = 0;
    /**
    *
    */
    public Shoulder() {
        shoulderMotor = new TalonFX(13, "rio");
        // BEGIN AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=CONSTRUCTORS

    // END AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=CONSTRUCTORS
            shoulderFrontSwitch = new DigitalInput(2);
        addChild("shoulderFrontSwitch", shoulderFrontSwitch);

        shoulderBackSwitch = new DigitalInput(3);
        addChild("shoulderBackSwitch", shoulderBackSwitch);

        // Voltage
        TalonFXConfiguration configs = new TalonFXConfiguration();
    
        // Current Limits
        configs.withCurrentLimits(
            new CurrentLimitsConfigs()
                .withStatorCurrentLimit(Amps.of(4))
                .withStatorCurrentLimitEnable(true));

        configs.Slot0.kP = Constants.ShoulderConstants.P; // An error of 1 rotation results in 2.4 V output
        configs.Slot0.kI = Constants.ShoulderConstants.I; // No output for integrated error
        configs.Slot0.kD = Constants.ShoulderConstants.D; // A velocity of 1 rps results in 0.1 V output
        // Peak output of 8 V
        configs.Voltage.withPeakForwardVoltage(Volts.of(8)).withPeakReverseVoltage(Volts.of(-8));
    

        // Torque
        configs.Slot1.kP = Constants.ShoulderConstants.P2; // An error of 1 rotation results in 60 A output
        configs.Slot1.kI = Constants.ShoulderConstants.I2; // No output for integrated error
        configs.Slot1.kD = Constants.ShoulderConstants.D2; // A velocity of 1 rps results in 6 A output
        // Peak output of 120 A
        configs.TorqueCurrent.withPeakForwardTorqueCurrent(Amps.of(120))
                .withPeakReverseTorqueCurrent(Amps.of(-120));
        

        MotionMagicConfigs motionMagicOn = configs.MotionMagic;
        motionMagicOn.withMotionMagicCruiseVelocity(RotationsPerSecond.of(20))
            .withMotionMagicAcceleration(RotationsPerSecondPerSecond.of(10))
            .withMotionMagicJerk(RotationsPerSecondPerSecond.per(Second).of(100));

 
        // Config Saving
        /* Retry config apply up to 5 times, report if failure */
        StatusCode status = StatusCode.StatusCodeNotInitialized;
        for (int i = 0; i < 5; ++i) {
            status = shoulderMotor.getConfigurator().apply(configs);
            if (status.isOK())
                break;
        }
        if (!status.isOK()) {
            System.out.println("Could not apply configs, error code: " + status.toString());
        }

        shoulderMotor.setPosition(0);
    }

    @Override
    public void periodic() {
        // This method will be called once per scheduler run
                if (isShoulderFrontTrip()) {
            shoulderMotor.setPosition(0);
        }

        if (isShoulderBackTrip()) {
            shoulderMotor.setPosition(270);
        }

        SmartDashboard.putNumber("Kraken Position", shoulderMotor.getPosition().getValueAsDouble());

    }

    @Override
    public void simulationPeriodic() {
        // This method will be called once per scheduler run when in simulation

    }

    // Put methods for controlling this subsystem
    // here. Call these from Commands.
    public void ShoulderStored() {
        shoulderMotor.setControl(m_motionMagicReq.withPosition(Constants.ShoulderConstants.PosStored));
        shoulderGoalPos = Constants.ShoulderConstants.PosStored;
    }

    public void ShoulderStationPickup() {
        shoulderMotor.setControl(m_motionMagicReq.withPosition(Constants.ShoulderConstants.PosStationPickup));
        shoulderGoalPos = Constants.ShoulderConstants.PosStationPickup;
    }

    public void ShoulderSeaBedPickup() {
        shoulderMotor.setControl(m_motionMagicReq.withPosition(Constants.ShoulderConstants.PosSeaBedPickup));
        // shoulderTarget = "Seabed";
        shoulderGoalPos = Constants.ShoulderConstants.PosSeaBedPickup;
    }

    public void ShoulderReef123() {
        shoulderMotor.setControl(m_motionMagicReq.withPosition(Constants.ShoulderConstants.PosReef123));
        shoulderGoalPos = Constants.ShoulderConstants.PosReef123;
    }

    public void ShoulderReef4() {
        shoulderMotor.setControl(m_motionMagicReq.withPosition(Constants.ShoulderConstants.PosReef4));
        shoulderGoalPos = Constants.ShoulderConstants.PosReef4;
    }

    public boolean isShoulderFrontTrip() {
        return shoulderFrontSwitch.get();
    }

    public boolean isShoulderBackTrip() {
        return shoulderBackSwitch.get();
    }

    public boolean isShoulderAtGoalPos() {
        return Math.abs(shoulderMotor.getPosition().getValueAsDouble() - shoulderGoalPos) < 1/(365*2); //half a degree
    }
}

